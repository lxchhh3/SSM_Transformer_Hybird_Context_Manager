[project]
name = "ctx-manager"
version = "0.0.1"
description = "Shared context layer for parallel CC-driven dev (store + MCP + Mamba SSM view)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
